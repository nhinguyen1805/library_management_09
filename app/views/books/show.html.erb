<% provide :title, t("book.show.title") %>
<h1><%= t "book.show.title" %></h1>
<%= link_to t("book.show.edit"), edit_book_path(@book) %>
<%= link_to t("book.show.delete"), @book,
  method: :delete, data: { confirm: t("book.show.confirm") } %>
